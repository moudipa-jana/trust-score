export type Categories = {
  id: string;
  title: string;
  about: string;
};

export type Category = {
  id: string;
  title: string;
  about: string;
};

export type userCategoty = {
  category: Category;
};
export type CategoryId = {
  categoryId: string;
};
